import java.time.LocalDateTime;

CREATE TABLE shoppinglist (
    list_name TEXT PRIMARY KEY,
    date TEXT as LocalDateTime
);

selectAll:
SELECT * FROM shoppinglist;

insert:
INSERT INTO shoppinglist (list_name, date)
VALUES (?, ?);

insertFull:
INSERT INTO shoppinglist (list_name, date)
VALUES ?;